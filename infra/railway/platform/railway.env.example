# Platform service variables (Railway)
# Copy these keys into Railway -> platform service -> Variables

# Required application dependencies
DATABASE_URL=postgres://postgres:<password>@<postgres-host>:5432/platform
REDIS_URL=redis://default:<password>@<redis-host>:6379
NATS_URL=nats://<nats-host>:4222
CLICKHOUSE_URL=http://<clickhouse-host>:8123

# Auth (pick one strategy)
JWT_SECRET=change-me-please
# JWT_PRIVATE_KEY_PEM=-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----
# JWT_PUBLIC_KEY_PEM=-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----

# Optional
RUST_LOG=info
# Disable AWS EC2 metadata probe in non-EC2 environments to reduce startup noise/timeouts
AWS_EC2_METADATA_DISABLED=true
